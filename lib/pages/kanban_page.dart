import 'package:flutter/material.dart';
import '../layout.dart';

class KanbanPage extends StatelessWidget {
  const KanbanPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Layout(child: Text('Kanban'));
  }
}
